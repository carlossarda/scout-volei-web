import React, {Component} from 'react';
import {Col} from "react-bootstrap";

export class Home extends Component{

    render = () => (
        <Col xs md={"10"}>
            <span>Bem vindo</span>
        </Col>
    );
}
